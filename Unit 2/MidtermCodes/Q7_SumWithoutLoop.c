/*
 * Q7_SumWithoutLoop.c
 *
 *  Created on: Jul 15, 2023
 *      Author: kollaly
 */

/*
#include <stdio.h>

int sum(int num);

int main(){

	setvbuf(stdout, NULL, _IONBF, 0);
	setvbuf(stderr, NULL, _IONBF, 0);

	printf("Summation from 1 to 100 without loop ( recursion ) is : %d ",sum(100));
	return 0;
}

int sum(int num){
	if(num == 1 ){
		return 1;
	}
	return sum(num-1)+num;
}
*/
